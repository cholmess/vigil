# Framework Integration Examples

Minimal setup snippets for common agent frameworks.

- `langchain_quickstart.py`
- `langgraph_swarm_quickstart.py`
- `openai_assistants_quickstart.py`
- `anthropic_quickstart.py`

All examples show where to call `VigilCanariWrapper.process_turn(...)` so incidents are persisted as `.bp.json` snapshots.
